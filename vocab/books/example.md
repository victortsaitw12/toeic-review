# 常錯字

<!--
單字本檔案：第一行的「# 標題」就是單字本名稱，下面用清單列出要收錄的單字。
單字必須是已存在單字卡裡的字（可先用 vocab/words/ 新增，再放進單字本）。

也可以直接放網站「單字本管理」匯出的 .md 檔（格式一樣），
或放匯出的 .json 檔（[{"name":"...","words":[...]}]）—— 匯入時都會自動辨識。
-->

- synergy
- leverage
- streamline
